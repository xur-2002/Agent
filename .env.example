# ===== Universal Ad CLI (required for scripts/generate_ad.py) =====
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL=groq/compound
LLM_API_KEY=
SERPER_API_KEY=
FEISHU_WEBHOOK_URL=

# Feishu Integration (REQUIRED)
# Example: https://open.feishu.cn/open-apis/bot/v2/hook/YOUR-TOKEN-HERE
FEISHU_MENTION=  # Optional: user_id to mention on failures

# LLM Provider Configuration
LLM_PROVIDER=groq  # Options: groq (free), openai (paid), dry_run (free, mock)
GROQ_API_KEY=  # Get from https://console.groq.com
GROQ_MODEL=llama-3.1-8b-instant  # Available models: llama-3.1-8b-instant, etc
OPENAI_API_KEY=  # Optional: fallback if Groq unavailable

# Search Integration
SERPER_API_KEY=  # Optional: for enriched search results

# State Persistence
PERSIST_STATE=local  # Options: local, repo, bitable
STATE_FILE=state.json  # Where to save task state (local mode)

# Concurrency & Reliability
MAX_CONCURRENCY=5
RETRY_COUNT=2
RETRY_BACKOFF=1s,3s,7s  # Exponential backoff timings

# Timezone & Execution
TIMEZONE=UTC
DRY_RUN=0  # Set to 1 to test without actually executing tasks

# GitHub (for repo persistence mode - requires GitHub Actions)
GITHUB_TOKEN=  # Automatically set in GitHub Actions

# Optional: Bitable Backend
# Only set these if you want to use Bitable for state storage
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_TABLE_APP_ID=
FEISHU_TABLE_TABLE_ID=
# ===== CONTENT PIPELINE (Direction 1) =====

# Search: Keyword Trend Watch (REQUIRED for content pipeline)
# Options: serper (recommended), bing
SEARCH_PROVIDER=serper
SERPER_API_KEY=  # Get from https://serper.dev/
# BING_SEARCH_KEY=  # Alternative if using Bing

# LLM: Article Generation (REQUIRED)
OPENAI_API_KEY=  # Get from https://platform.openai.com/

# Email Delivery (REQUIRED)
SMTP_HOST=smtp.gmail.com  # Example: Gmail, or your corporate SMTP
SMTP_PORT=587  # 587 for TLS, 465 for SSL
SMTP_USER=  # Email address or username
SMTP_PASS=  # Email password or app-specific password
SMTP_TO=recipient1@example.com,recipient2@example.com  # Comma-separated recipients

# Image Generation (OPTIONAL, uses OpenAI Images API)
# Uses OPENAI_API_KEY above
# Skipped gracefully if API limitation

# Content Generation Config
CONTENT_DRAFTS_DIR=drafts  # Where to save article drafts
CONTENT_PUBLISH_KITS_DIR=publish_kits  # Where to save publish kits