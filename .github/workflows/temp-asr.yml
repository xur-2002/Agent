name: Temp ASR

on:
  push:
    branches:
      - temp-asr-20260910
    paths:
      - 'asr/READY'

permissions:
  contents: write

jobs:
  transcribe:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: temp-asr-20260910
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install ffmpeg
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg
      - name: Reassemble and decode candidate audio
        run: |
          cat asr/cand1/p*.txt > asr/cand1/input.b64
          base64 -d asr/cand1/input.b64 > asr/cand1/input.c2
          ffmpeg -hide_banner -loglevel error -i asr/cand1/input.c2 -ar 16000 -ac 1 asr/cand1/input.wav
          ls -lh asr/cand1/input.c2 asr/cand1/input.wav
      - name: Install ASR
        run: |
          python -m pip install --upgrade pip
          pip install faster-whisper
      - name: Transcribe Mandarin
        run: |
          cat > /tmp/transcribe.py <<'PY'
          from faster_whisper import WhisperModel
          OFFSET=2130.0
          model = WhisperModel('small', device='cpu', compute_type='int8')
          segments, info = model.transcribe('asr/cand1/input.wav', language='zh', vad_filter=True, beam_size=5)
          lines=[]
          for s in segments:
              line=f'[{s.start+OFFSET:.2f}-{s.end+OFFSET:.2f}] {s.text.strip()}'
              print(line)
              lines.append(line)
          open('asr/cand1/transcript.txt','w',encoding='utf-8').write('\n'.join(lines)+'\n')
          PY
          python /tmp/transcribe.py
      - name: Save transcript
        run: |
          git config user.name 'temp-asr-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add asr/cand1/transcript.txt
          git commit -m 'Write candidate ASR transcript' || exit 0
          git push origin HEAD:temp-asr-20260910
      - name: Upload transcript artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: candidate-transcript
          path: asr/cand1/transcript.txt
          if-no-files-found: ignore
